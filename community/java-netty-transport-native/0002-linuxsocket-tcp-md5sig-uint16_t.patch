musl does not define u_int16_t without extra BSD compatibility headers; use C99 uint16_t.

--- a/src/main/c/netty_epoll_linuxsocket.c
+++ b/src/main/c/netty_epoll_linuxsocket.c
@@ -21,6 +21,7 @@
  */
 #define _GNU_SOURCE
 
+#include <stdint.h>
 #include <stdlib.h>
 #include <string.h>
 #include <errno.h>
@@ -546,7 +547,7 @@ static void netty_epoll_linuxsocket_setTcpMd5Sig(JNIEnv* env, jclass clazz, jint
             netty_unix_errors_throwIOException(env, "key is too long");
             return;
        }
-        md5sig.tcpm_keylen = (u_int16_t) keylen;
+        md5sig.tcpm_keylen = (uint16_t) keylen;
         (*env)->GetByteArrayRegion(env, key, 0, keylen, (void *) &md5sig.tcpm_key);
         if ((*env)->ExceptionCheck(env) == JNI_TRUE) {
             return;
